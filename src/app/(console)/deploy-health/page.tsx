import DeployHealthView from "@/components/DeployHealthView";

export const metadata = { title: "Deploy health · Cantila Console" };

export default function DeployHealthPage() {
  return <DeployHealthView />;
}
