import { buildPageMetadata } from "@/lib/seo";
export const metadata = buildPageMetadata({
  title: "Platform services",
  description: "The database, storage, mailbox, and number every Cantila project ships with — wired into the env before the first build runs.",
  path: "/docs/auto-wired",
  type: "article",
});

# Platform services

Every project deployed on Cantila gets its own dedicated database, object
storage, an email service, and an SMS number — provisioned together with
the project and wired into the environment before the first build runs.

This page is the overview. Each service has its own page with the API
surface and current phase status:

- [Databases & storage](/docs/databases) — managed Postgres + S3-compatible buckets.
- [Domains](/docs/domains) — search, register, and attach custom domains.
- [Mail](/docs/mail) — first-party email provider.
- [SMS, Voice & Numbers](/docs/sms) — telephony provider and number marketplace.
- [Automations](/docs/automations) — managed n8n / OpenClaw + native workflow canvas.
- [Connections](/docs/connections) — account-wide credentials reused across workflows.
- [Agents](/docs/agents) — the self-healing brain and agent swarm.
- [Templates](/docs/templates) — one-shot create + deploy marketplace.

## What's wired, by phase

| Service           | Env var(s)                                              | Phase     | Default state                  |
| ----------------- | ------------------------------------------------------- | --------- | ------------------------------ |
| Postgres          | `DATABASE_URL`                                          | Live      | Real, running on Hetzner       |
| Object storage    | `STORAGE_ENDPOINT`, `STORAGE_BUCKET`, `STORAGE_KEY`     | Live      | S3-compatible, per-project     |
| Mailbox + SMTP    | `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`      | Phase 2   | Real creds, stub MTA today     |
| SMS number        | `SMS_FROM`, `SMS_API_KEY`                               | Phase 3   | Stub adapter today             |
| Cantila API key   | `CANTILA_API_KEY` (scoped to the project)               | Live      | Used by the runtime SDK        |

<Callout tone="warn" title="What works today">
  - **Live:** Postgres (`DATABASE_URL`) and S3-compatible storage are real and running.
  - **Phase 2:** Mailbox + per-project SMTP credentials are real and auto-wired, but no real bytes leave the MTA yet — the send path is a stub. See [Mail](/docs/mail).
  - **Phase 3:** The SMS number is a stub adapter — outbound/inbound carrier delivery flips on as the phase ships. See [SMS](/docs/sms).
</Callout>

`Phase 2` and `Phase 3` env vars exist today against the stub adapters —
you can write code that targets them and it will work against the API; the
live carrier delivery flips on as each phase ships. See the
[changelog](/changelog).

## How the wiring works

When you create a project:

1. **Project row** is created in the control plane.
2. **Database** is provisioned in the managed Postgres cluster. A per-
   project user and database are created; `DATABASE_URL` is generated.
3. **Mailbox** is created at `m_<random>@cantila.app` (Phase 2 — this
   becomes `<project>@<your-domain>` once Mail goes live).
4. **SMS number** is leased from the inventory (Phase 3 — until then a
   placeholder routes inbound to the project webhook).
5. **Cantila API key** scoped to the project is minted.
6. **Env injection.** All of the above are written to the project's
   secrets, marked `cantila-managed: true`, and locked from user
   overwrite.
7. **First build** runs with the env in place.

## Sleeping when idle

Auto-wired services follow the same sleep policy as the project itself:

- **Hobby** projects sleep when idle (~10 minutes of no traffic). Their
  database connections close; storage buckets stay live. Wake on next
  request.
- **Starter / Pro / Agency** projects pinned always-on stay live.

A sleeping database doesn't bill on the meter; storage and number leases
keep their fixed monthly cost regardless of sleep.

## Tearing down

Deleting a project tears down its auto-wired services in lockstep:

```text
project       deleted
db            dropped
storage       bucket emptied + dropped
mailbox       deleted (Phase 2)
sms number    released (Phase 3)
domain        retained (your domain, your call)
```

The domain stays — it's a registrar product, billed separately from the
project. Manage from **Console → Domains**.

## Why bundled

The whole point: a builder ships an app and `DATABASE_URL` already
resolves. No second vendor signup. No glue code. One bill.

<Callout tone="info">
  Want to bring your own Postgres / S3 / mailbox? Override the
  Cantila-managed env vars at the project level — Cantila respects user
  overwrites and stops managing the slot until you delete the override.
</Callout>
