# Build plan — see the canonical plan

The single source of truth for Cantila's product and build plan is:

**`../Cantila_Complete_Plan.md`** — *Cantila — Complete Product & Build Plan* (v1.3)

Implementation status — what's built vs. still mocked across the three
projects (`cantila-control-plane`, `cantila-console`, `cantila-cli`) — is
tracked there in **§15 — Implementation Status**, kept updated as work lands.

This file is intentionally a stub, to avoid two competing copies of the plan.
